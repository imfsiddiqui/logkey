theme: jekyll-theme-cayman
repository_name: logkey
title: logkey
description: A lightweight Python application to log user inputs into a CSV file with Docker support for easy deployment and GitHub Actions to automate key workflows.
show_downloads: true
collections:
  pages:
    output: true          # Generates HTML files for each file
    permalink: /:path/    # URL structure for collection items
